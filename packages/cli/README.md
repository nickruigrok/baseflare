# @baseflare/cli

Placeholder CLI package for Baseflare.

## Intended Usage

Bootstrap new projects with the full package name:

```bash
npx @baseflare/cli init my-app
```

After `init`, the generated app should add `@baseflare/cli` to `devDependencies` so day-to-day
commands run locally via `bf` or `baseflare`.
